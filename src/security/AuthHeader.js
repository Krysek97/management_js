// import Cookies from "universal-cookie";


// function AuthHeader() {
//     const cookie = new Cookies;
//     const user = cookie.get('token');
  
//     if (user && user.cookie) {
//       return { "Authorization": 'Bearer ' + user.cookie };
//     } else {
//       return {};
//     }
//   }

//   export default Cookies;
  